import { styled } from "@material-ui/core/styles";
import { CardMedia } from "@material-ui/core";

export const ImgCardMedia = styled(CardMedia)({
  height: 200,
  width: 200,
  marginBottom: '0.5rem',
  marginLeft: '0.5rem'
});
